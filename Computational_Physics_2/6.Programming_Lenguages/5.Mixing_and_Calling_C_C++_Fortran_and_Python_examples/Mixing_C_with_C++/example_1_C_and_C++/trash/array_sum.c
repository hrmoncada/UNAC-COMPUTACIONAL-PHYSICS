#include<stdio.h>

// function to return array sum and sum of elements in arrays of size n
int sum(int a[], int b[], int n) {
    int sum = 0; // initialize sum
 
// Iterate through all elements and add them to sum
    for (int i = 0; i < n; i++){
         c[i] = a[i] + b[i];
         sum += c[i];
    }
    
    return sum;
}

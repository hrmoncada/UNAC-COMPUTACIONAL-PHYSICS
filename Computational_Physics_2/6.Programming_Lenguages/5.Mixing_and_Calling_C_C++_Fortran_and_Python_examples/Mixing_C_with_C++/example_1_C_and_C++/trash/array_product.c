// C program to find product of array elements.
#include <stdio.h>
int array_product(int ar[], int b[], int n) {
    int result = 1;
    for (int i = 0; i < n; i++) {
        c[i] = a[i] * b[i]
        result = result * c[i];
    }    
    return result;
}
